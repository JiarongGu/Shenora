<Project Sdk="Microsoft.NET.Sdk">

  <!--
    THE PORTABILITY PROOF for D20, and it works by what it CANNOT reference.

    This is plain `net10.0` — no `-windows`, no UseWindowsForms — and it references only
    Shenora.Core + Shenora.Ipc. So an app facade here can pick a file, use the clipboard, open a URL
    and marshal to the UI thread, and STILL compile with no Windows dependency: exactly the claim D20
    makes, checked by the compiler on every build instead of asserted in a document.

    If someone later drags a Windows type into one of those contracts, or moves a contract back into
    Shenora.Windows, THIS PROJECT GOES RED. That is the entire point — it is a guard, not a demo.
    It is in Shenora.slnx so `dev.mjs build|verify` compiles it (P5.5 H5 put the samples in the
    solution; without that this guard would never run).

    Never packable, like every sample.
  -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <CodePage>65001</CodePage>
    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <!-- Deliberately NOT Shenora.Windows or Shenora.Windows. Adding either would defeat the guard. -->
    <ProjectReference Include="..\..\src\Shenora.Core\Shenora.Core.csproj" />
    <ProjectReference Include="..\..\src\Shenora.Ipc\Shenora.Ipc.csproj" />
    <!--
      Shenora.Media, referenced from PORTABLE logic on purpose — this is the D41 tripwire being ARMED.
      This project is net10.0, so it turns RED the moment a platform type reaches app logic; that is what
      enforces "app logic names Shenora.Media and never Shenora.Media.{Platform}". Until this reference
      existed the tripwire was green for reasons unrelated to media, i.e. a tripwire that could not fail —
      the state `phase-workflow.md` forbids. Its facade USES the planner, so removing the portability
      would break the build rather than a comment.
    -->
    <ProjectReference Include="..\..\src\Shenora.Media\Shenora.Media.csproj" />
  </ItemGroup>

</Project>
