<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>true</IsPackable>
    <PackageId>Shenora.Ipc</PackageId>
    <Description>Shenora IPC: transport-neutral typed request/response/notification envelopes, a composable middleware dispatcher with module facades, structured error contract (code + parameters, i18n-ready), and System.Text.Json serialization defaults for React ⇄ .NET desktop communication.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Shenora.Core\Shenora.Core.csproj" />
  </ItemGroup>

</Project>
