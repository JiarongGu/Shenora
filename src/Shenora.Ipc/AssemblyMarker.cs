namespace Shenora.Ipc;

// This assembly's public surface arrives with the Phase 3 IPC extraction (docs/ROADMAP.md).
// Until then it exposes no public types — enforced by the API-surface baseline tests.
internal static class AssemblyMarker;
