# surface-lexicon.txt — the words a PUBLIC TYPE NAME in Shenora is allowed to be built from.
#
# WHY THIS IS AN ALLOW-LIST AND NOT A BLOCKLIST OF BUSINESS WORDS.
# The owner's standing review criterion for this repo is "make sure this is a LIBRARY — we are not
# solving specific business logic; everything here has to be generic enough that any of our
# applications can adopt it". That is the single most important property the kit has, and until this
# gate it was the only invariant with no tripwire at all: `ApiSurfaceTests` detects that the surface
# CHANGED, never that a change was a good idea, and its documented workflow is to copy the `.actual`
# file over the baseline — so it waves domain vocabulary straight through.
#
# A blocklist ("Invoice", "Playlist", "Skin"…) cannot work here for two reasons. It is open-ended —
# it only ever catches the domain words someone already thought of, and the leak that matters is the
# one from the app nobody anticipated. And writing down the domain nouns of the private sibling apps
# would itself put what those apps DO into a public repo (`.claude/rules/sensitive-info.md`).
#
# Inverting it fixes both. Every word below is shell, platform, transport or hosting vocabulary. A
# new public type built only from these words cannot be domain-specific; a type that introduces an
# unknown word stops the build and a human decides which it is. When the answer is "this really is
# generic", add the word here — that edit is the review, and it is cheap next to the baseline update
# and CHANGELOG entry a new public type already needs.
#
# SCOPE: public TYPE names only. That is exactly what D22 governs ("every public type is named for
# its MECHANISM"), and it is the highest-signal, lowest-noise slice of the surface. Members are not
# swept — deliberately, so this gate keeps the property doc-drift was built on: precise checks that
# cannot cry wolf beat broad ones that get switched off.
#
# Derived 2026-08-01 from the 147 public types then shipping: 134 words, every one of them a
# mechanism. The kit passed its own criterion on the day the gate was written; the gate exists to
# keep it that way through the adoptions that have not happened yet.
#
# Format: one word per line, PascalCase segment as it appears in a type name. `#` starts a comment.
# A leading `I` on an interface is stripped before splitting, so `IClipboardService` needs only
# Clipboard + Service.

# ── The product itself ────────────────────────────────────────────────────────────────────────────
Shenora

# ── Platform / tech nouns (the "using the tech we have" half) ─────────────────────────────────────
Api
App
Application
Browser
Clipboard
Cookie
Dpi
File
# `Files` (2026-08-03) is the kit's file-writing entry point — one letter from System.IO.File on
# purpose. Every write it does is atomic, which is why there is no `Atomic` word here any more: that
# framed correctness as an opt-in mode, and the call sites that forget an opt-in are the ones that
# break.
Files
Folder
Form
Forms
Ipc
Json
# `Mobile` (2026-08-02) is the platform FAMILY the two mobile packages share, and it replaced `Maui`
# the same day: the shell ships as Shenora.Android + Shenora.iOS, whose common source is
# src/Shenora.Mobile/. Naming the types after the vendor framework would have described the
# implementation rather than the thing — and the two faces run on entirely different engines
# (Chromium WebView on Android, WKWebView on iOS), so `Maui` was the only word true of neither.
# Gated by MetadataSurfaceTests rather than SurfaceVocabularyTests — the Windows test project cannot
# reference a net10.0-android assembly — so both gates read this one lexicon.
Mobile
Path
Paths
Shell
Ui
Url
View
View2
Web
Win
Window
Windows

# ── Windows desktop presentation — the frameless-chrome cluster ───────────────────────────────────
Button
Caption
Colors
Icon
Menu
Optimized
Panel
Splash
Tray
Viewport
Maximizable
Secondary
Single
Instance

# ── Messaging / transport mechanisms ──────────────────────────────────────────────────────────────
Batch
Bridge
Bus
Call
Categories
Command
Dispatcher
Event
Events
Message
Middleware
Notification
Payload
Pump
Request
Response
Route
Router
Scheme

# ── Hosting, composition and lifecycle ────────────────────────────────────────────────────────────
# `Headless` (2026-08-02) is hosting vocabulary, not a scenario: it names a host with no UI loop,
# which is what Core needed so `Run()` stops being Windows-only in practice. Note it does NOT mean
# "mobile" — a platform that owns its own loop needs its own runner (see HeadlessHostExtensions).
Base
Bootstrap
Builder
# `Capability` (2026-08-02) names what a SHELL can or cannot honour — hosting vocabulary, and the
# word the cross-shell rule is actually about (ShellCapability.NotSupported). Note it is NOT a
# feature flag: absent means "no expression of this exists on this platform".
Capability
Headless
Callback
Collection
Container
Context
Controller
Environment
Extensions
Facade
Guard
Helper
Hook
Host
Lifecycle
Manager
Module
Pool
Provider
Registry
Root
Runner
Scoped
Service
Store

# ── Work, state and results ───────────────────────────────────────────────────────────────────────
Argument
Arguments
End
Ended
Error
Codes
Exception
Info
Kind
Label
Operation
Operations
Options
Progress
Reason
Report
Result
Source
State
Status
Target
Unhandled

# ── Input, rendering and streaming ────────────────────────────────────────────────────────────────
Action
Custom
Deferred
Drop
Embedded
Frame
Input
Interaction
Interactive
Key
Launcher
Pointer
Region
Render
Resource
Session
Streaming
Text
Wheel
# `Write` (2026-08-03) is in FileWriteMode — the enum names the PROCESS (how a write reaches the
# target) rather than a kind of file. Atomic is the default; Direct is the opt-out.
Write
Zone

# ── Filesystem updates (2026-08-02) ─────────────────────────────────────────────────────────────
# The file-update queue: serialized mutations, per-update atomicity. Update and Change are
# mechanism nouns — what the queue applies — and Atomicity is the property an app chooses per
# update. Note what is still NOT here: no archive, download, install or sync noun.
#
Atomicity
Change
Create
# `Manifest` + `Diff` (2026-08-02, staged application updates). A manifest is a list of files with
# sizes and hashes and a diff is a comparison of two of them — packaging vocabulary, and the exact
# words BOTH donor apps independently used. Note what is still NOT here and should be argued for on
# its own: no Release, Download, Install or Patch noun. The kit ships the changeset, not the updater.
Diff
Manifest
# `Zip` (2026-08-05) — the CONTAINER FORMAT, in ZipUpdateSource. Admitted against the two warnings above,
# and the argument is that it is narrower than either of them rather than an exception to them:
#   - It is a format the BCL already names (System.IO.Compression.ZipArchive), not a product concept. The
#     type reads zip entries and does nothing else — it never downloads, never installs, never decides what
#     a release IS. Where the archives come from stays the app's, exactly as IUpdateSource intends.
#   - `Archive` would have been the WORSE name here, which is the part worth keeping: it is the generic
#     noun the section above warns about, and it would over-claim — this reads zips, not tar or 7z. Naming
#     the format is the honest, smaller commitment.
# ⚠ Still no Release, Download, Install or Patch noun, and this line is not a licence for one. A
# `DownloadingUpdateSource` or an `Installer` would be the kit growing the updater it deliberately does not
# ship — argue those on their own merits.
#
# `Extraction` + `Limits` (2026-08-05) arrived with Shenora.IO.Compression, and the package's NAME is what
# kept `Archive` off this list: because the namespace already says what these operate on, the types are
# `ExtractionResult`/`ExtractionLimits` rather than `Archive…`. Worth recording, because the first attempt
# named the package `Shenora.Archives` and added `Archive` here — directly contradicting the bullet above,
# in the same file, on the same day. **A package name that has to be explained by its type names is the
# wrong package name**; naming it after the framework's own area (System.IO.Compression) made the types
# smaller and the lexicon smaller at once.
Zip
Extraction
Limits
# `Pack` (2026-08-06) — a NAMED, VERSIONED SET OF FILES an app needs on disk at runtime, delivered as one
# archive. Admitted because it names the unit, not its contents: the first consumer's pack holds a media
# engine, but a model, a font set, a voice pack or a fixture tree are the same mechanism, and nothing on
# `ResourcePack`'s surface knows which it is holding.
# It also passes the harder test in `generic-library.md` — *would a second app disagree with this?* The
# four things it decides (where a version lives, whether it is complete, how a path resolves inside it,
# when the old one is collected) are not app decisions; the BYTES are, and the kit never supplies them.
# ⚠ Watch it the way `Media` and `Download` are watched. `Pack` must keep meaning the container. A
# `PackDownloader`, `PackRegistry` or `PackStore` would be the kit growing delivery — deciding where packs
# come from — which is precisely the half D42 leaves with the app, and must be argued afresh.
Pack
Delete
Directory
Entry
Holder
Inspector
Journal
Stage
Step
Undo
Lease
Lock
Locker
Manager
Move
Replace
# `Replacement` (2026-08-03) is the noun form of Replace, already here as the queue's FileChange.
# It names the in-flight swap that Files.BeginReplace hands back — what the object IS, rather
# than what the caller is using it for. It replaced `FileTransform`, which described the caller's
# activity (encoding, compiling) instead of the thing, and would have drifted the moment someone
# used it for something that is not a transform.
Replacement
Restart
Update

# ── Data shapes ───────────────────────────────────────────────────────────────────────────────────
Byte
Dialog
Dialogs
Filter
Mapping
Range

# ── Mission scheduling (0.3.0; the prefix was `Work` until 2026-08-02) ────────────────────────────
# Added for the mission scheduler + filesystem claim layer. Every word here is concurrency or
# scheduling vocabulary — the gate was run before the code was written and admitted these one at a
# time, which is the review it exists to force. Note what is NOT here: no job, task-type, download,
# archive, scan or import noun. Those are the APPLICATION's words and the reason the kit ships a
# scheduler rather than a job system.
#
# `Definition` (what should run) and `Execution` (one specific run) are the definition/execution
# split: they replaced `Request`, `Context`, `View` and `Snapshot` on this surface, which were four
# names for two concepts.
#
# `Mission` replaced `Work` (owner's call): `Work` is too common a word to grep or to own, and the
# obvious alternative `Task` collides with System.Threading.Tasks.Task — and `TaskScheduler` would
# have been ambiguous against the BCL type in every consumer that imports both namespaces.
# `Mission` names a unit of work with an objective and an outcome, is unique on this surface, and is
# still MECHANISM vocabulary rather than any app's domain noun (`Quest` was rejected for reading as
# domain vocabulary in a games family).
Chain
Claim
Claims
Definition
Execution
Flat
Lane
Mode
Nested
Observer
Outcome
Policy
Priority
Queue
Record
Recovery
Retry
Mission
Scheduler
Scope
Step

# ── Watch this one ────────────────────────────────────────────────────────────────────────────────
# Download: a browser mechanism (a session observing a download), NOT a product feature. It is the
# closest thing on the surface to a domain word, and it is allowed only because `DownloadHit` names
# something the WebView2 session layer genuinely observes. A `DownloadManager`/`DownloadQueue` would
# be the kit growing a product, and should be argued for on its own merits — not waved past because
# this line already exists.
Download
Hit

# ── Media (Shenora.Media, 2026-08-03) ─────────────────────────────────────────────────────────────
# Each of these names a MECHANISM, which is the only reason they are allowed (D22). The test that
# earns the section: `MediaPlaybackPlanner` decides what must HAPPEN to a file — the mechanism — while
# WHICH codecs are playable is `MediaPlaybackPolicy`, supplied by the app. The kit ships no codec list,
# so no format name ever reaches this surface.
#
# ⚠ Watch this one the way `Download` above is watched. "Media" is the closest thing here to a domain
# word, and it is allowed because the kit genuinely serves a media EXCHANGE (containers and streams it
# never decodes). A `MediaLibrary`, `MediaPlayer` or `Track` would be the kit growing a product and must
# be argued on its own merits, not waved past because this line exists.
Media
Playback
Plan
Planner
Policy
Action
Cache
Key
Probe
Result
Stream
Info
Kind
# `Conversion` (2026-08-05, DM3) — turning one encoding into another. A mechanism noun, and admitted for the
# same reason `MediaPlaybackPlanner` was: the kit decides what must HAPPEN and never how to encode anything.
# `MediaConversionOptions.Convert` is the APP's delegate (D42, the engine is never vendored), so what these
# four types actually name is the COMPOSITION around it — a mission for the long run, a path claim so one
# source converts once, an atomic replace, and a content key so a replaced source invalidates its output.
# ⚠ Watch it the way `Media` and `Download` are watched. A `Transcoder`, `Encoder`, or a filter `Ladder`
# would be the kit growing an engine, which the design puts explicitly out of scope (§2) — argue those
# afresh rather than waving them past because this line exists.
Conversion
# `Segment` + `Run` + `Engine` (2026-08-06) — the segment-stream route, harvested from a consuming app that
# proved it on a device. All three name MECHANISM and none names a product:
#   · `Segment` is a piece of a stream produced on a fixed grid. Streaming-protocol vocabulary (HLS and DASH
#     both use it), not ours and not any app's.
#   · `Run` is one live production — started, writes numbered output, can be killed. Deliberately not `Job`
#     or `Task`: `Task` collides with the BCL, and `Job` is the app's word for work it schedules, which is
#     the noun the mission layer refused for the same reason.
#   · `Engine` names the SEAM an app plugs its own encoder into — `ISegmentEngine`. ⚠ This entry used to
#     say the kit "ships no implementation and never will (D42, D51)", which D52 corrected LATER THE SAME
#     DAY: the kit does ship a DEFAULT, assembled from a managed remux and the platform's own codecs. That
#     narrows D42 rather than reversing it — D42's objection was always VENDORING (megabytes every consumer
#     pays for, a licence every consumer inherits), and a default costing zero bytes and zero obligations
#     is neither. The word is still admissible for the better reason: it names a MECHANISM, and the seam
#     remains the escape hatch for reach the default lacks rather than the only door.
# ⚠ Watch `Engine` the way `Media` and `Download` are watched, and closer: the moment a type here DECIDES
# how to encode rather than asking the app, the kit has grown an engine, which D42 puts out of scope. A
# `SegmentEncoder`, `SegmentTranscoder`, or a codec ladder in `src/` would be that — argue those afresh
# rather than waving them past because this line exists.
Segment
Run
Engine
# `Matroska` (2026-08-06) — the CONTAINER FORMAT, in MatroskaProbe. Admitted on exactly the terms `Zip` was,
# and the parallel is close enough to be worth stating: it is a published format name rather than a product
# concept, the type reads its header and does nothing else, and naming the format is the SMALLER commitment.
# `MediaContainerProbe` would over-claim — it reads Matroska and not MP4 — and the Archives lesson above is
# that a name which has to be explained by its contents is the wrong name.
# ⚠ The watch here is scope, not vocabulary: this may only ever READ a header. A `MatroskaWriter`,
# `MatroskaMuxer` or anything that produces one is a different argument (it is the translation layer's
# output side) and must be made on its own, not waved past because the format is already named.
Matroska
# `Mp4` + `Remuxer` (2026-08-07) — THE ARGUMENT THE LINE ABOVE ASKED FOR, made on its own terms.
# `Mp4Remuxer` reads Matroska and writes MP4. Note first what it is NOT: not a `MatroskaWriter`, which is
# what that warning was actually guarding against — nothing here produces a Matroska file, and the kit
# still owns exactly one direction.
#   · `Mp4` is a published container format name, admitted on precisely the terms `Zip` and `Matroska`
#     were: it names the format the type emits and nothing about a product. A `MediaRemuxer` or
#     `ContainerRewriter` would OVER-CLAIM in the way `MediaContainerProbe` would have — this reads one
#     container and writes one other, and the Archives lesson is that a name needing its contents
#     explained is the wrong name.
#   · `Remuxer` is the kit's OWN decided vocabulary already: `MediaPlaybackAction.Remux` has been the
#     planner's verdict since 2026-08-03. This type is the thing that performs the verdict the planner
#     already names, so the word is not new to the surface — only to the part of it this gate sweeps
#     (D22 is types; enum members are not swept).
# ⚠ And the scope test it has to keep passing (D52): a remux DECODES NOTHING. It copies frames and their
# decoder configuration verbatim into a different box — zero bytes shipped, zero licence weight, no codec
# written. The moment something here decodes or encodes a frame, the kit has grown the engine D42 puts out
# of scope, and `Mp4Transcoder` or a `Decoder` would be that — argue those afresh.
Mp4
Remuxer
# `Writer` (2026-08-07) — in `IMediaContainerWriter`, the MUXER seam. Admitted because it names a MECHANISM
# and nothing else: something that writes a container. The reasoning for wanting the seam at all is worth
# recording because it is not the obvious one — `MediaConversionOptions.Convert` already accepts a delegate,
# but that delegate is ALL-OR-NOTHING: a consumer wanting only their own AAC encoder, or only a native
# muxer, had to reimplement demux, timing, sample tables and interleave behind it. Cutting the pipeline into
# replaceable PARTS is the point (owner: "the issue is not the speed, its how to optimize this").
# ⚠ Watch it the way `Engine` is watched: a `Writer` that starts DECIDING what to encode has become an
# engine, which D42 puts out of scope. This one is TOLD what to carry and refuses what it cannot.
Writer
# `Audio` (2026-08-07) — in `IMediaAudioConversion`, and admitted because the name had to become MORE
# specific rather than less. It was `IMediaStreamConversion`, which OVER-CLAIMED: its run exposes
# SampleRate and Channels, so it only ever converted audio, and a name that promises streams while
# delivering audio is the same defect as a scenario name — it invites the next contributor to add video to
# something shaped wrong for it. Owner's direction, same day: make the interfaces "more specific and more
# generic" — specific in what each says it does, generic in the shapes that have to extend.
# It is platform vocabulary rather than domain vocabulary: every OS names this surface the same way
# (AVAudioSession, MediaCodec's `audio/*`, SystemMediaTransportControls), which is the `ProfileDirectory`
# test — genuine mechanism words the platforms themselves use.
# ⚠ Watch it the way `Media` is watched. `AudioLibrary` or an `AudioEffect` would be the kit growing a
# product; this converts ONE stream so a webview can play it, and nothing more.
# ⚠⚠ This note originally listed `AudioPlayer` among the words that would mean the kit had grown a product,
# and eight days later `IMediaPlayer` was admitted (below). Read the argument there before concluding the
# gate was simply overridden — the line moved deliberately, and it moved because D54 changed what the kit
# is FOR, not because the check was inconvenient.
Audio

# `Player` (2026-08-07) — in `IMediaPlayer`, and admitted only after arguing against the warning THIS FILE
# had already written about it (see the `Audio` note above, which named `AudioPlayer` as the shape that
# would mean the kit had grown a product). The gate fired on exactly the word it was built to catch, so the
# case has to be made rather than waved through.
#
# WHAT CHANGED IS THE PRODUCT DEFINITION, not the tolerance for domain words. D54 (owner, 2026-08-07):
# the kit is a hybrid app framework whose differentiator is native .NET capability, and its job is the
# translation layer for "what .NET can do and React cannot". A host-owned player is the clearest instance
# of that: iOS PAUSES a <video> the moment the app backgrounds, and AVPlayer does not. No amount of
# JavaScript closes that gap.
#
# WHY IT IS MECHANISM AND NOT DOMAIN, on the file's own tests:
#   · The `ProfileDirectory` test — do the platforms themselves use the word? AVPlayer (Apple), ExoPlayer
#     and MediaPlayer (Android), MediaPlayer (Windows). All three name this surface `Player`. It is
#     platform vocabulary in the same way `Audio` is.
#   · It names a MECHANISM: something that opens a source and advances a clock through it. Compare the
#     names it is NOT — `MediaLibrary`, `Playlist`, `NowPlayingQueue`, `AudioEffect` — every one of which
#     describes a PRODUCT built ON a player.
#
# ⚠ THE LINE, stated so the next contributor does not have to re-derive it. `IMediaPlayer` ships ONE
# source, open/play/pause/seek/close, and no queue, playlist, gapless, crossfade, shuffle or effects —
# the same refusal that keeps a queue model out of IPlaybackSession, for the same reason: only the app
# knows what "next" means. **A `Next()` on this interface is the tell that it has become a product.**
Player

# ── Resource interception (Shenora.Core, 2026-08-04) ──────────────────────────────────────────────
# The interceptor is a MIDDLEWARE pipeline over resource requests — the same shape IMessageDispatcher
# already is for messages, applied to bytes instead. All mechanism words; `Delivery` in particular names
# a measured PLATFORM fact (whether the platform or the handler applies a Range offset — D44).
Interceptor
Middleware
Handler
Delivery
# `Pipeline` (2026-08-04) is the composition itself, now a public type because all three shells share it
# rather than hand-rolling the back-to-front chain build three times.
Pipeline

# ── System transport / Now Playing (Shenora.Core, 2026-08-04) ─────────────────────────────────────
# `IPlaybackSession` and friends publish to the OS's media transport surface (lock screen, media
# notification, SystemMediaTransportControls) and receive its commands back. Every word was already here
# for other reasons — Playback, Session, Progress, Info, State, Command, Request — which is itself the
# evidence that the naming stayed on mechanism vocabulary. Only the PLURAL is new: `PlaybackCommands` is
# the flags SET of controls the OS should offer, distinct from the single `PlaybackCommand` it sends back.
#
# ⚠ Note what is deliberately NOT here: Artist, Album, Track, Artwork. The record's fields are Title /
# Subtitle / GroupName precisely so this contract can sit in Shenora.Core — which every package
# references — without putting music vocabulary on the surface of an app that has none. Fields are not
# swept by this gate, so that discipline is a review matter; it is written down here so the next person
# renaming them for convenience meets the reason first.
Commands

# ── Live status surfaces (Shenora.Core + Shenora.iOS, 2026-08-04) ─────────────────────────────────
# `ILiveActivities` / `LiveActivityState` put a long-running job on the OS-rendered strip an app gets
# while backgrounded — iOS's Dynamic Island and lock screen today. `Live` and `Activity` are both
# mechanism words: a live surface is one the OS keeps updating, and an activity is a unit of work in
# progress. Neither names a product concept, and the state record carries Title / Subtitle / Progress
# with no domain vocabulary at all.
#
# ⚠ `Activity` is the word to watch here, for the same reason `Media` and `Download` are watched above:
# it is Apple's and Android's term as well as a generic one, so a type that starts meaning "an
# ActivityKit thing" rather than "a unit of work" has drifted and must be argued afresh.
Live
Activity
Activities
# Core resource serving, re-layered 2026-08-04 (D45). `Derived` names WHAT is cached (anything derived
# from a source file) rather than the feature that caches it — the family shares this helper.
Content
Types
Derived
Files
Options
Extensions

# ── File dialogs, split per method (2026-08-05) ────────────────────────────────────────────────
# `Open` and `Save` are the only VERBS in this file, and they were admitted deliberately rather than
# renamed around. Three things decided it:
#   1. They are the PLATFORM's own vocabulary, not ours — `OpenFileDialog` and `SaveFileDialog` are
#      BCL type names, and every shell expresses the same pair (ACTION_CREATE_DOCUMENT,
#      UIDocumentPickerViewController's export mode).
#   2. `IFileDialogs` has used them as METHOD names since P5.5. Members are not swept by this gate,
#      which is the only reason this never fired before — so the words were already the kit's, and
#      what changed is that the split PROMOTED them to type names.
#   3. Every mechanism-noun alternative was worse. `FileSourceOptions`/`FileTargetOptions` are
#      buildable from words already here, and they mis-describe the folder case: a folder pick is a
#      SOURCE when it grants a working directory and a TARGET when it chooses an export destination,
#      so the honest name for that one is neither.
# ⚠ Watch these the way `Media` and `Download` are watched. A verb on this surface is a step toward
# naming ACTIONS rather than mechanisms (D22). These two are anchored to a specific OS dialog that
# every platform ships; a `LoadOptions`, `ImportOptions` or `ExportOptions` would not be, and must be
# argued on its own merits rather than waved past because this section exists.
Open
Save
# -- Archives (Shenora.Archives, 2026-08-05) ------------------------------------------------------
# Safe extraction, split out of Core because "I am building an app that unpacks archives" is a boundary a
# CONSUMER experiences (D37's own test) while a phone app hosting a page is not one.
# Archive is admitted here after being warned against in the filesystem-updates section above, and the
# warning still stands for what it was about: the kit shipping an UPDATER. This package does not — it puts
# files where you asked and refuses the ones that would land elsewhere. Extraction names that mechanism,
# Limits names the bounds a caller sets on it.
# Watch: a Downloader, Installer or Patcher here would be exactly the growth the older note forbids.
# -- Safe area (2026-08-05) -------------------------------------------------------------------------
# The window insets a page must not draw under. These three are admitted as PLATFORM vocabulary, not
# kit invention, and that is the whole argument: the words are the ones every platform already uses for
# this exact thing — CSS says `env(safe-area-inset-*)`, UIKit says `safeAreaInsets`, Android says
# `WindowInsets` — so an adopter meets them before they meet Shenora. A kit-invented synonym
# (`ChromeMargin`, `EdgeReserve`) would be a name only this library uses for a concept the platform
# already named, which is the opposite of what D22 asks for.
# `Insets` is the measurement, `Safe`+`Area` the region; `Script` was already here.
# ⚠ Watch: this is a LAYOUT word, and layout is where a headless kit is most tempted to grow opinions
# (D13). Insets are a fact the platform reports and the kit relays. A `Theme`, `Layout`, `Margin` or
# `Padding` on this surface would be the kit deciding how a page LOOKS, and must be argued on its own
# merits rather than waved past because these three are here.
# `Script` joins them for `SafeAreaScript`, the pure builder that produces the snippet a shell injects.
# Web-hosting vocabulary the kit already speaks everywhere else (injected scripts, script injection on
# the desktop host); it names the ARTIFACT produced, which is what makes the type testable without a
# webview at all.
Safe
Area
Insets
Script